class M(idm)
    @idm = idm
class S(id,m)
    @id = id
    @m = m
class Person(id,m)
    @id = id
    @m = m
    @function hello(s)
        text = "Hello! I'm "
        text.append(self.name)
        print(text)
        print(s)
        return
global
(
    true = 1 > 0
    false = 1 < 0
    pass = 0
    m = M(0)
si = S(0,m)
p = Person(0,si)
int = 100
)
function n()
    return
print("start")
function f(arg)
    m2 = M(10)
    s2 = S(10,m2)
    p2 = Person(10,s2)
    if true
    (
        m3 = M(30)
        s3 = S(30,m3)
        p3 = Person(30,s3)

        p2 <- p3

        //p2.id <- p3.id
        //p2.m.id <- p3.m.id
        //p2.m.m.idm <- p3.m.m.idm
        
    )
    print(int_to_str(p2.m.m.idm))
    return

f(p)
//print(int_to_str(p.m.m.idm))
